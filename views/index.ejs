<%- include('./partials/header') %>

<div class="min-h-screen flex flex-col md:flex-row px-5 md:px-20">
    <!-- Error Message -->
    <% if (error.length > 0) { %>
        <div class="absolute top-5 left-1/2 -translate-x-1/2 p-3 rounded-md bg-rose-500">
            <span class="inline-block text-white">
                <%= error %>
            </span>
        </div>
    <% } %>

    <!-- Registration Form Section -->
    <div class="w-full md:w-1/2 flex items-center justify-center py-10">
        <div class="w-full px-5 sm:px-10 md:px-16 lg:px-20">
            <h3 class="text-3xl md:text-4xl font-semibold">
                 <span class="text-indigo-500">LoginAndSignUp</span>
            </h3>
            <h4 class="text-lg md:text-xl mt-3 mb-5">Create Your Account</h4>
            <form autocomplete="off" action="/register" method="post">
                <input
                    class="block bg-gray-50 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-gray-300"
                    type="text" placeholder="Full Name" name="fullname">
                <input
                    class="block bg-gray-50 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-gray-300"
                    type="email" placeholder="Email" name="email">
                <input
                    class="block bg-gray-50 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-gray-300"
                    type="password" placeholder="Password" name="password">
                <input
                    class="block w-full px-5 py-2 bg-teal-500 text-white rounded-full hover:bg-teal-600 cursor-pointer"
                    type="submit" value="Create My Account">
            </form>
        </div>
    </div>

    <!-- Login Form Section -->
    <div class="w-full md:w-1/2 flex items-center justify-center py-10">
        <div class="w-full px-5 sm:px-10 md:px-16 lg:px-20">
            <h4 class="text-lg md:text-xl mb-5">Login to Your Account</h4>
            <form autocomplete="off" action="/login" method="post">
                <input
                    class="block bg-gray-50 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-gray-300"
                    type="email" placeholder="Email" name="email">
                <input
                    class="block bg-gray-50 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-gray-300"
                    type="password" placeholder="Password" name="password">
                <input
                    class="block w-full px-5 py-2 bg-teal-500 text-white rounded-full hover:bg-teal-600 cursor-pointer"
                    type="submit" value="Login">
            </form>
        </div>
    </div>
</div>

<%- include('./partials/footer') %>
